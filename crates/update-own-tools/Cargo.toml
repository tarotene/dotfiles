[package]
name = "update-own-tools"
version = "0.1.0"
edition.workspace = true
license.workspace = true
publish.workspace = true

[dependencies]
serde = { version = "1.0.229", features = ["derive"] }
toml = { version = "1.1.6", default-features = false, features = ["parse", "serde"] }

[dev-dependencies]
tempfile = "3.27.0"
